// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://dennisinfoseccookbook.github.io',
    // 유저 페이지(루트)라면 base 설정을 두지 않습니다.
});
